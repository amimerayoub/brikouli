import { LocateFixed, Minus, Plus } from "lucide-react";
export function MapControls({ onLocate, onZoomIn, onZoomOut, disabled = false }: { onLocate: () => void; onZoomIn: () => void; onZoomOut: () => void; disabled?: boolean }) { return <div className="map-controls" aria-label="عناصر تحكم الخريطة"><button type="button" onClick={onLocate} disabled={disabled} aria-label="العودة إلى موقعي"><LocateFixed size={20} /></button><div><button type="button" onClick={onZoomIn} disabled={disabled} aria-label="تكبير"><Plus size={19} /></button><button type="button" onClick={onZoomOut} disabled={disabled} aria-label="تصغير"><Minus size={19} /></button></div></div>; }

import { Search } from "lucide-react";
import { useState } from "react";
import { AdminShell } from "@/components/admin/AdminShell";
import { trpc } from "@/lib/trpc";
export default function AdminAuditLogs() { const [query, setQuery] = useState(""); const audits = trpc.brikouli.admin.audit.useQuery({ page: 1, pageSize: 30, query }); return <AdminShell title="سجل التدقيق" description="سجل إداري للعمليات الحساسة، متاح للمسؤولين فقط."><section className="admin-toolbar"><label><Search size={16} /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="ابحث باسم الإجراء" aria-label="البحث في سجل التدقيق" /></label></section><section className="admin-table-panel"><table><thead><tr><th>الوقت</th><th>المسؤول</th><th>الإجراء</th><th>الهدف</th><th>سياق آمن</th></tr></thead><tbody>{audits.data?.success ? audits.data.data.items.map(event => <tr key={event.id}><td>{new Date(event.createdAt).toLocaleString("ar-MA")}</td><td>{event.actor?.fullName || "نظام/حساب محذوف"}</td><td><b>{event.action}</b></td><td>{event.targetType}</td><td><code>{JSON.stringify(event.metadata)}</code></td></tr>) : <tr><td colSpan={5} className="admin-table-empty">{audits.isLoading ? "جارٍ تحميل السجل…" : "لا توجد عمليات مطابقة."}</td></tr>}</tbody></table></section></AdminShell>; }

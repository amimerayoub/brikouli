import { AnimatePresence, motion } from "framer-motion";
import { AppButton } from "./AppButton";
export function ConfirmationDialog({ open, title, description, confirmLabel = "تأكيد", onClose, onConfirm }: { open: boolean; title: string; description: string; confirmLabel?: string; onClose: () => void; onConfirm: () => void }) { return <AnimatePresence>{open && <motion.div className="dialog-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.section role="alertdialog" aria-modal="true" className="confirmation-dialog" initial={{ opacity: 0, scale: .96, y: 14 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: .96, y: 14 }} transition={{ duration: .22 }}><h2>{title}</h2><p>{description}</p><div><AppButton variant="secondary" onClick={onClose}>إلغاء</AppButton><AppButton onClick={onConfirm}>{confirmLabel}</AppButton></div></motion.section></motion.div>}</AnimatePresence>; }

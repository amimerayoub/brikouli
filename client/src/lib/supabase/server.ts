/** Supabase server-client preparation only. Server functionality is outside Phase 1 scope. */
export const supabaseServerConfig = { urlEnvKey: "NEXT_PUBLIC_SUPABASE_URL", anonKeyEnvKey: "NEXT_PUBLIC_SUPABASE_ANON_KEY", configured: false } as const;
